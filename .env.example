# 复制为 .env 后填写（.env 不会进 git）

# Let's Encrypt / Caddy（docker-compose.https.yml）
DOMAIN=gw2rally.duckdns.org
ACME_EMAIL=d80924gw2@gmail.com

# DuckDNS 动态解析（docker-compose.duckdns.yml）
DUCKDNS_SUBDOMAIN=gw2rally
DUCKDNS_TOKEN=

# Cloudflare Tunnel（docker-compose.tunnel.yml，方案 A 可留空）
CLOUDFLARE_TUNNEL_TOKEN=
